function init(x) {
    let total = 0;
    sum(x);
    return total;   
    
    function sum(input) {
        for (i = 0; i < input; i++) {
            if (i % 3 === 0 || i % 5 === 0) {
                total += i;
            } else {conitnue;}
        }
    }
}